#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always o (Success)
 */

int main(void)
{
	char c;

	c = 'z';
	while
		(c >= 'a') {
			putchar(c);
			c++;
		}
	putchar('\n');
	return (0);
}
