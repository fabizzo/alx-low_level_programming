#include "main.h"
#include <ctype.h>
#include <stdio.h>

/**
 * main - uppercase letters
 *
 *@c: char to check
 *
 * Return: 0 or 1
 */

int _isupper(void)
{
	int ch = 'C';

	if (c >= 'A' && c <= 'Z' ) 
		return (1);
	else
		return (0);
}
